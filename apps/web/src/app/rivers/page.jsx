import RiversReservoirs from '@/features/RiversReservoirs/RiversReservoirs';

export const metadata = { title: 'Rivers & Reservoirs — FloodGuard' };

export default function Page() {
  return <RiversReservoirs />;
}
