import SavedLocations from '@/features/SavedLocations/SavedLocations';

export const metadata = { title: 'Saved Locations — FloodGuard' };

export default function Page() {
  return <SavedLocations />;
}
