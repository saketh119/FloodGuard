import WeatherRainfall from '@/features/WeatherRainfall/WeatherRainfall';

export const metadata = { title: 'Weather & Rainfall — FloodGuard' };

export default function Page() {
  return <WeatherRainfall />;
}
